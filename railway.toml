# Serviço Frota (web) — Dockerfile na raiz do monorepo
# Monorepo: api → services/api/railway.toml | gt06 → services/gt06-ingest/railway.toml
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
healthcheckPath = "/login"
healthcheckTimeout = 120
