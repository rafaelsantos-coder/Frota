# Config as Code — serviço Railway "gt06-ingest"
# Settings → Config file path: railway.gt06.toml

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.gt06"

[deploy]
restartPolicyType = "ON_FAILURE"
